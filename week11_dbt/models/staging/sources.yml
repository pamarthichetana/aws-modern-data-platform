version: 2

sources:
  - name: raw
    schema: main
    tables:
      - name: users
        description: "Raw user data from JSONPlaceholder API"
      - name: todos
        description: "Raw todos data used as orders proxy"
